#pragma once
// Copy to secrets.h locally. Never publish this file with real secrets.
constexpr char WIFI_SSID[]="";
constexpr char WIFI_PASSWORD[]="";
constexpr char MQTT_PASSWORD[]="";
constexpr char SMS_PHONE[]=""; // optional, international format; leave ENABLE_SMS=false until a recipient is configured
